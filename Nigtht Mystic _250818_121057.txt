-- Night Mystic By Mauro & Araújo7x -- Hub unificado (Fluent): Auto Farm Level + Kill Mobs Nearby + BringMob + Tools + Auto Stats + Teleports (Tween + Stop) + Chests & Fruits + Fruit ESP --========================[ Serviços / Variáveis ]========================-- local Players = game:GetService("Players") local ReplicatedStorage = game:GetService("ReplicatedStorage") local TweenService = game:GetService("TweenService") local RunService = game:GetService("RunService") local Workspace = game:GetService("Workspace") local Lighting = game:GetService("Lighting") local UserInputService = game:GetService("UserInputService") local CoreGui = game:GetService("CoreGui") local Player = Players.LocalPlayer local DialogueEvent = ReplicatedStorage.BetweenSides.Remotes.Events.DialogueEvent local CombatEvent = ReplicatedStorage.BetweenSides.Remotes.Events.CombatEvent local ToolEvent = ReplicatedStorage.BetweenSides.Remotes.Events.ToolsEvent local StatsEvent = (ReplicatedStorage.BetweenSides.Remotes.Events:FindFirstChild("StatsEvent")) local QuestsNpcs = Workspace.IgnoreList.Int.NPCs.Quests local Enemys = Workspace.Playability.Enemys local Map = Workspace:FindFirstChild("Map") local QuestDescriptions = require(ReplicatedStorage.MainModules.Essentials.QuestDescriptions) local CFrameAngle = CFrame.Angles(math.rad(-90), 0, 0) -- Estado local OnFarmLevel = false local OnKillNearby = false local BringMobEnabled = false local FastAttackEnabled = false local AutoEquipEnabled = false local AutoStatsEnabled = false local AutoStoreEnabled = false local AutoTouchEnabled = false local ESP_Enabled = false -- Config local Settings = { TweenSpeed = 270, FarmTick = 0.05, HoverY = 17, -- Distância acima do inimigo BringSize = 35, -- AOE para "prender" os mobs SelectedStat = "Strength", SelectedTool = nil, KillRadius = 120, -- Raio para Kill Mobs Nearby } -- Helpers local function IsAlive(char) if not char then return false end local h = char:FindFirstChildOfClass("Humanoid") return (h and h.Health > 0) end local function safeWaitForHRP() local char = Player.Character or Player.CharacterAdded:Wait() return char:WaitForChild("HumanoidRootPart"), char end --========================[ Indexação de Inimigos ]========================-- local EnemiesFolders = {} for _, islandFolder in ipairs(Enemys:GetChildren()) do if islandFolder:IsA("Folder") then EnemiesFolders[islandFolder.Name] = islandFolder end end --========================[ Tween Teleport Suave (com controle global) ]========================-- local activeTween = nil local function CancelActiveTween() if activeTween then pcall(function() activeTween:Cancel() end) activeTween = nil end end local function TweenTo(TargetCFrame, speed) local hrp, char = safeWaitForHRP() if not hrp or not IsAlive(char) then return end local dist = (hrp.Position - TargetCFrame.Position).Magnitude local duration = (dist / (speed or Settings.TweenSpeed)) CancelActiveTween() if duration < 0.05 then hrp.CFrame = TargetCFrame return end activeTween = TweenService:Create(hrp, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = TargetCFrame}) activeTween:Play() activeTween.Completed:Once(function() activeTween = nil end) end --========================[ Detecção de Level / Quest Atual ]========================-- local function GetPlayerLevel() local mainUI = Player.PlayerGui:FindFirstChild("MainUI") if not mainUI then return 1 end local mainFrame = mainUI:FindFirstChild("MainFrame") if not mainFrame then return 1 end local stats = mainFrame:FindFirstChild("StastisticsFrame") or mainFrame:FindFirstChild("StatisticsFrame") if not stats then return 1 end local levelBG = stats:FindFirstChild("LevelBackground") if levelBG then local levelText = levelBG:FindFirstChild("Level") if levelText and tonumber(levelText.Text) then return tonumber(levelText.Text) end end for _, d in ipairs(stats:GetDescendants()) do if d:IsA("TextLabel") and d.Text:match("^%d+$") then local num = tonumber(d.Text) if num and num >= 1 and num <= 5000 then return num end end end return 1 end local function BuildQuestList() local list = {} for _, q in pairs(QuestDescriptions) do if q.Goal and q.Goal > 1 then table.insert(list, {Level = q.MinLevel, Target = q.Target, NpcName = q.Npc, Id = q.Id}) end end table.sort(list, function(a,b) return a.Level > b.Level end) return list end local QuestList = BuildQuestList() local function PickQuestForLevel() local lvl = GetPlayerLevel() for _, q in ipairs(QuestList) do if q.Level <= lvl then return q end end end local function HasQuestFor(targetName) local ok, res = pcall(function() local QuestFrame = Player.PlayerGui.MainUI.MainFrame.CurrentQuest if not QuestFrame.Visible then return false end local text = nil local goal = QuestFrame:FindFirstChild("Goal") if goal and goal.Text then text = goal.Text end if not text then for _, d in ipairs(QuestFrame:GetDescendants()) do if d:IsA("TextLabel") and d.Text and d.Text ~= "" then if d.Text:find("Defeat") or d.Text:find("/") then text = d.Text; break end end end end return text and text:find(targetName) ~= nil end) return ok and res end local function TakeQuest(npcName, questId) local npc = QuestsNpcs:FindFirstChild(npcName, true) local root = npc and npc.PrimaryPart if root then DialogueEvent:FireServer("Quests", {NpcName = npcName, QuestName = questId}) TweenTo(root.CFrame * CFrame.new(0,0,15)) task.wait(1.2) end end --========================[ Busca de Inimigos / Combate ]========================-- local function filterEnemiesInFolder(folder, enemyName) local found = {} for _, e in ipairs(folder:GetChildren()) do local nm = (e:GetAttribute("OriginalName") or e:GetAttribute("EnemyName") or e.Name) if nm == enemyName or tostring(nm):find(enemyName) then local hum = e:FindFirstChild("Humanoid") local hrp = e:FindFirstChild("HumanoidRootPart") if hum and hrp and hum.Health > 0 then local ready = true if e:GetAttribute("Respawned") ~= nil then ready = e:GetAttribute("Respawned") end if e:GetAttribute("Ready") ~= nil then ready = ready and e:GetAttribute("Ready") end if ready then table.insert(found, e) end end end end return found end local function getAllEnemiesByName(enemyName) local all = {} local cached = EnemiesFolders[enemyName] if cached and cached.Parent then local t = filterEnemiesInFolder(cached, enemyName) for _, e in ipairs(t) do table.insert(all, e) end else for _, island in ipairs(Enemys:GetChildren()) do local t = filterEnemiesInFolder(island, enemyName) if #t > 0 then EnemiesFolders[enemyName] = island for _, e in ipairs(t) do table.insert(all, e) end end end end return all end local function closestEnemy(enemyName) local list = enemyName and getAllEnemiesByName(enemyName) or (function() local all = {} for _, island in ipairs(Enemys:GetChildren()) do for _, e in ipairs(island:GetChildren()) do table.insert(all, e) end end return all end)() local hrp = (Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")) if not hrp or #list == 0 then return end local best, dist = nil, math.huge for _, e in ipairs(list) do local r = e:FindFirstChild("HumanoidRootPart") local h = e:FindFirstChild("Humanoid") if r and h and h.Health > 0 then local d = (hrp.Position - r.Position).Magnitude if d < dist then dist, best = d, e end end end return best end local function swing() local char = Player.Character if not IsAlive(char) then return end local tool = char:FindFirstChildOfClass("Tool") if not tool then return end pcall(function() tool:Activate() local handle = tool:FindFirstChild("Handle") if handle then if handle:FindFirstChild("Cooldown") then handle.Cooldown.Value = 0 end if handle:FindFirstChild("AttackCooldown") then handle.AttackCooldown.Value = 0 end if handle:FindFirstChild("Debounce") then handle.Debounce.Value = false end local s = handle:FindFirstChildOfClass("Sound"); if s then s:Play() end end ToolEvent:FireServer("Effects", 1) ToolEvent:FireServer("Activate", 1) if FastAttackEnabled then for _=1,3 do tool:Activate(); task.wait(0.01) end end end) end local function multiHit(enemies) local t = workspace:GetServerTimeNow() CombatEvent:FireServer("DealDamage", { CallTime = t, DelayTime = 0, Combo = 1, Results = enemies, Damage = math.random(50,150), CriticalHit = (math.random(1,10) <= 3) }) end --========================[ Anti-Dano / NoClip durante o Farm/Nearby ]========================-- local BV = Instance.new("BodyVelocity") BV.Velocity = Vector3.zero BV.MaxForce = Vector3.new(1e9,1e9,1e9) BV.P = 1000 local function HoverActive() return OnFarmLevel or OnKillNearby end local canCollideParts = {} local function trackPart(p) if p:IsA("BasePart") and p.CanCollide then table.insert(canCollideParts, p) end end local function untrackPart(p) local i = table.find(canCollideParts, p) if i then table.remove(canCollideParts, i) end end local function attachBV() local char = Player.Character if not IsAlive(char) then return end local upper = char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso") if HoverActive() and upper and BV.Parent ~= upper then BV.Parent = upper elseif (not HoverActive()) and BV.Parent then BV.Parent = nil end end local function setNoClip() local char = Player.Character if not char then return end for _, bp in ipairs(canCollideParts) do bp.CanCollide = not HoverActive() and true or false end end Player.CharacterAdded:Connect(function(char) canCollideParts = {} char.DescendantAdded:Connect(trackPart) char.DescendantRemoving:Connect(untrackPart) for _,d in ipairs(char:GetDescendants()) do trackPart(d) end end) RunService.Stepped:Connect(function() if IsAlive(Player.Character) then attachBV() setNoClip() if BV.Parent and BV.Velocity ~= Vector3.zero then BV.Velocity = Vector3.zero end end end) --========================[ BringMob (insta) ]========================-- local function BringMob(enemyNameOrList, targetCFrame) if not BringMobEnabled then return 0 end local all = {} if typeof(enemyNameOrList) == "string" then all = getAllEnemiesByName(enemyNameOrList) elseif typeof(enemyNameOrList) == "table" then all = enemyNameOrList end local brought = 0 for _, e in ipairs(all) do local hrp = e:FindFirstChild("HumanoidRootPart") local hum = e:FindFirstChild("Humanoid") if hrp and hum and hum.Health > 0 then if not hrp:FindFirstChild("BodyVelocity") then local bv = Instance.new("BodyVelocity", hrp) bv.Velocity = Vector3.zero bv.MaxForce = Vector3.one * math.huge end hrp.CanCollide = false hrp.Size = Vector3.new(Settings.BringSize, Settings.BringSize, Settings.BringSize) hrp.CFrame = targetCFrame brought += 1 end end if brought > 0 then pcall(sethiddenproperty, Player, "SimulationRadius", math.huge) end return brought end --========================[ Auto Farm Level (hover + quest) ]========================-- local function AutoFarmLevelLoop() OnFarmLevel = true task.spawn(function() while task.wait(Settings.FarmTick) and OnFarmLevel do if not IsAlive(Player.Character) then repeat task.wait(0.5) until IsAlive(Player.Character) task.wait(0.2) end local q = PickQuestForLevel() if not q then continue end if not HasQuestFor(q.Target) then TakeQuest(q.NpcName, q.Id) continue end local enemy = closestEnemy(q.Target) if not enemy then continue end local ehrp = enemy:FindFirstChild("HumanoidRootPart") local ehum = enemy:FindFirstChild("Humanoid") if ehrp and ehum and ehum.Health > 0 then if BringMobEnabled then BringMob(q.Target, ehrp.CFrame) end local target = ehrp.CFrame * CFrame.new(0, Settings.HoverY, 0) TweenTo(target, Settings.TweenSpeed) local all = getAllEnemiesByName(q.Target) if #all > 0 then multiHit(all) end swing() end end end) end --========================[ Kill Mobs Nearby (hover + raio) ]========================-- local function GetEnemiesWithinRadius(radius) local hrp = (Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")) if not hrp then return {} end local found = {} for _, island in ipairs(Enemys:GetChildren()) do for _, e in ipairs(island:GetChildren()) do local h = e:FindFirstChild("Humanoid") local r = e:FindFirstChild("HumanoidRootPart") if h and r and h.Health > 0 then local d = (hrp.Position - r.Position).Magnitude if d <= (radius or Settings.KillRadius) then table.insert(found, e) end end end end return found end local function KillNearbyLoop() OnKillNearby = true task.spawn(function() while task.wait(Settings.FarmTick) and OnKillNearby do if not IsAlive(Player.Character) then repeat task.wait(0.5) until IsAlive(Player.Character) task.wait(0.2) end local enemies = GetEnemiesWithinRadius(Settings.KillRadius) if #enemies == 0 then continue end local focus = closestEnemy() if not focus then continue end local ehrp = focus:FindFirstChild("HumanoidRootPart") local ehum = focus:FindFirstChild("Humanoid") if ehrp and ehum and ehum.Health > 0 then if BringMobEnabled then BringMob(enemies, ehrp.CFrame) end local target = ehrp.CFrame * CFrame.new(0, Settings.HoverY, 0) TweenTo(target, Settings.TweenSpeed) multiHit(enemies) swing() end end end) end --========================[ Auto Stats ]========================-- local function AutoStatsLoop() AutoStatsEnabled = true task.spawn(function() while AutoStatsEnabled do if StatsEvent then local payload = { "UpgradeStat", { Defense = (Settings.SelectedStat=="Defense") and 1 or 0, Sword = (Settings.SelectedStat=="Sword") and 1 or 0, Gun = (Settings.SelectedStat=="Gun") and 1 or 0, Strength = (Settings.SelectedStat=="Strength") and 1 or 0, DevilFruit = (Settings.SelectedStat=="DevilFruit") and 1 or 0 } } pcall(function() StatsEvent:FireServer(unpack(payload)) end) end task.wait(0.1) end end) end --========================[ FUNÇÕES "IDK": Chest/Fruit/Store/ESP ]========================-- local function tryFireTouch(part) if not AutoTouchEnabled or not part or not part:IsA("BasePart") then return end local char = Player.Character or Player.CharacterAdded:Wait() for _, tx in ipairs(part:GetChildren()) do if tx:IsA("TouchTransmitter") then for _, my in ipairs(char:GetDescendants()) do if my:IsA("BasePart") then firetouchinterest(my, part, 0) task.wait() firetouchinterest(my, part, 1) end end end end end local function scanWorldForTouch() pcall(function() for _, v in ipairs(Workspace:GetDescendants()) do if v:IsA("BasePart") then local n = v.Name:lower() if n:find("chest") or n:find("fruit") or n:find("devil") or n:find("spawned") then tryFireTouch(v) end end end end) end local function AutoStoreLoop() AutoStoreEnabled = true task.spawn(function() while AutoStoreEnabled do pcall(function() ToolEvent:FireServer("StoreFruit") end) task.wait(0.25) end end) end -- Fruit ESP local ESP_Color = Color3.fromRGB(255,255,255) local ESP_Adorns = {} local function clearESP() for obj, adorn in pairs(ESP_Adorns) do if adorn and adorn.Parent then adorn:Destroy() end ESP_Adorns[obj] = nil end end local function addBillboard(part, text) local bb = Instance.new("BillboardGui") bb.Size = UDim2.new(0, 120, 0, 32) bb.AlwaysOnTop = true bb.Adornee = part bb.Name = "NM_FruitESP" local tl = Instance.new("TextLabel", bb) tl.Size = UDim2.new(1,0,1,0) tl.BackgroundTransparency = 1 tl.Text = text tl.Font = Enum.Font.GothamBold tl.TextScaled = true tl.TextColor3 = ESP_Color return bb end local function scanFruitESP() if not ESP_Enabled then return end for _, v in ipairs(Workspace:GetDescendants()) do if v:IsA("BasePart") then local n = v.Name:lower() if n:find("fruit") or n:find("devil") then if not ESP_Adorns[v] then local bb = addBillboard(v, "FRUIT") bb.Parent = v ESP_Adorns[v] = bb end end end end for obj, adorn in pairs(ESP_Adorns) do if not obj.Parent then if adorn and adorn.Parent then adorn:Destroy() end ESP_Adorns[obj] = nil end end end --========================[ UI (Fluent) ]========================-- -- (pegamos um snapshot do CoreGui/gethui antes pra identificar qual ScreenGui é do Fluent) local GuiRoot = (gethui and gethui()) or CoreGui local beforeSet = {} for _,c in ipairs(GuiRoot:GetChildren()) do beforeSet[c] = true end local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))() local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))() local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))() local Window = Fluent:CreateWindow({ Title = "Night Mystic By Mauro & Araújo7x", SubTitle = "Vox Seas", TabWidth = 160, Size = UDim2.fromOffset(620, 460), Acrylic = false, Theme = "Darker", MinimizeKey = Enum.KeyCode.LeftShift }) -- identificar ScreenGui recém-criada pelo Fluent local FluentGui -- ScreenGui que contém o hub for _,c in ipairs(GuiRoot:GetChildren()) do if c:IsA("ScreenGui") and not beforeSet[c] then FluentGui = c break end end local Tabs = { Main = Window:AddTab({ Title = "Main", Icon = "swords" }), Tools = Window:AddTab({ Title = "Tools", Icon = "axe" }), Stats = Window:AddTab({ Title = "Stats", Icon = "bar-chart-4" }), Teleports = Window:AddTab({ Title = "Teleports", Icon = "map" }), Collect = Window:AddTab({ Title = "Chests & Fruits", Icon = "cherry" }), Discord = Window:AddTab({ Title = "Discord", Icon = "message-circle" }), SettingsTab = Window:AddTab({ Title = "UI Settings", Icon = "sliders" }) } local Options = Fluent.Options --========================[ MAIN ]========================-- Tabs.Main:AddToggle("AutoFarmLevel", { Title = "Auto Farm Level", Default = false }):OnChanged(function(v) if v then AutoFarmLevelLoop() else OnFarmLevel = false end end) Tabs.Main:AddToggle("KillNearby", { Title = "Kill Mobs Nearby (raio)", Default = false }):OnChanged(function(v) if v then KillNearbyLoop() else OnKillNearby = false end end) Tabs.Main:AddToggle("BringMob", { Title = "Bring Mob (instantâneo)", Default = false }):OnChanged(function(v) BringMobEnabled = v end) Tabs.Main:AddToggle("FastAttack", { Title = "Fast Attack", Default = false }):OnChanged(function(v) FastAttackEnabled = v end) Tabs.Main:AddSlider("TweenSpeed", { Title = "Tween Speed", Default = Settings.TweenSpeed, Min = 120, Max = 600, Rounding = 0 }):OnChanged(function(v) Settings.TweenSpeed = v end) Tabs.Main:AddSlider("HoverY", { Title = "Altura acima do inimigo (Farm & Nearby)", Default = Settings.HoverY, Min = 5, Max = 40, Rounding = 0 }):OnChanged(function(v) Settings.HoverY = v end) Tabs.Main:AddSlider("KillRadius", { Title = "Raio Kill Nearby", Default = Settings.KillRadius, Min = 40, Max = 300, Rounding = 0 }):OnChanged(function(v) Settings.KillRadius = v end) --========================[ TOOLS ]========================-- local ToolDropdown = Tabs.Tools:AddDropdown("ToolDropdown", { Title = "Selecionar Tool", Values = {}, Multi = false }) local AutoEquipToggle = Tabs.Tools:AddToggle("ToolAutoEquip", { Title = "Auto Equip Tool", Default = false }) local function refreshTools() local list = {} local bp = Player:FindFirstChild("Backpack") if bp then for _, t in ipairs(bp:GetChildren()) do if t:IsA("Tool") then table.insert(list, t.Name) end end end ToolDropdown:SetValues(list) end Tabs.Tools:AddButton({ Title = "Refresh Tools", Callback = refreshTools }) refreshTools() local equipping = false local function EquipSelectedToolLoop() if equipping then return end equipping = true task.spawn(function() while Options.ToolAutoEquip.Value do local selected = ToolDropdown.Value Settings.SelectedTool = selected if selected then local bp = Player.Backpack if bp then local tool = bp:FindFirstChild(selected) local char = Player.Character if tool and char and char:FindFirstChild("Humanoid") then pcall(function() char.Humanoid:EquipTool(tool) end) end end end task.wait(0.1) end equipping = false end) end AutoEquipToggle:OnChanged(function(v) AutoEquipEnabled = v if v then EquipSelectedToolLoop() end end) --========================[ TOOLS - NoFog (toggle manual) ]========================-- local function setNoFog(enabled) if enabled then Lighting.FogEnd = 1e9 Lighting.FogStart = 0 Lighting.FogColor = Color3.fromRGB(255, 255, 255) local atm = Lighting:FindFirstChildOfClass("Atmosphere") if atm then atm.Density = 0 atm.Offset = 0 atm.Glare = 0 atm.Haze = 0 end else Lighting.FogEnd = 1000 Lighting.FogStart = 0 Lighting.FogColor = Color3.fromRGB(192, 192, 192) local atm = Lighting:FindFirstChildOfClass("Atmosphere") if atm then atm.Density = 0.3 atm.Offset = 0.25 atm.Glare = 0 atm.Haze = 0.25 end end end Tabs.Tools:AddToggle("NoFog", { Title = "No Fog (remover neblina)", Default = false }):OnChanged(function(v) setNoFog(v) end) --========================[ STATS ]========================-- Tabs.Stats:AddDropdown("StatSelect", { Title = "Atributo", Values = {"Strength","Defense","Sword","Gun","DevilFruit"}, Default = "Strength", Multi = false }):OnChanged(function(v) Settings.SelectedStat = v end) Tabs.Stats:AddToggle("AutoStats", { Title = "Auto Stats", Default = false }):OnChanged(function(v) if v then AutoStatsLoop() else AutoStatsEnabled = false end end) --========================[ TELEPORTS (Tween + lista dinâmica + Stop) ]========================-- local islands = {} if Map then for _, island in ipairs(Map:GetChildren()) do if island:FindFirstChild("Base") then table.insert(islands, island.Name) end end end if #islands == 0 then islands = {"Town","Jungle","Desert","Marine","Sky","Prison","Magma","Underwater","Colosseum"} end local IslandDropdown = Tabs.Teleports:AddDropdown("IslandDropdown", { Title = "Selecione a Ilha", Values = islands, Multi = false, Default = islands[1] }) Tabs.Teleports:AddButton({ Title = "Teleportar (Tween)", Callback = function() local sel = IslandDropdown.Value if sel and Map then local island = Map:FindFirstChild(sel) if island and island:FindFirstChild("Base") then local base = island.Base local target = base:FindFirstChild("HumanoidRootPart") and base.HumanoidRootPart.CFrame or (base.WorldPivot * CFrame.new(0,5,0)) TweenTo(target, Settings.TweenSpeed) end end end }) Tabs.Teleports:AddButton({ Title = "Stop Teleport", Callback = function() CancelActiveTween() end }) --========================[ CHESTS & FRUITS (Touch / Store / ESP) ]========================-- Tabs.Collect:AddToggle("AutoChestFruit", { Title = "Auto Chest & Auto Fruit (touch)", Default = false }):OnChanged(function(v) AutoTouchEnabled = v if v then task.spawn(function() while Options.AutoChestFruit.Value do scanWorldForTouch() task.wait(0.75) end end) end end) Tabs.Collect:AddToggle("AutoStoreFruit", { Title = "Auto Store Fruits", Default = false }):OnChanged(function(v) if v then AutoStoreLoop() else AutoStoreEnabled = false end end) Tabs.Collect:AddToggle("FruitESP", { Title = "Fruit ESP / Notifier", Default = false }):OnChanged(function(v) ESP_Enabled = v if not v then clearESP() return end task.spawn(function() while Options.FruitESP.Value do scanFruitESP() task.wait(1.0) end end) end) --========================[ DISCORD (apenas infos, sem convite) ]========================-- Tabs.Discord:AddSection("About") Tabs.Discord:AddParagraph({ Title = "Night Mystic", Content = "Hub com Farm Level (hover), Kill Nearby (raio), BringMob, Tools (NoFog), Stats, Teleports (Tween + Stop), Chests/Fruits e Fruit ESP." }) --========================[ SAVE / UI SETTINGS ]========================-- SaveManager:SetLibrary(Fluent) InterfaceManager:SetLibrary(Fluent) SaveManager:IgnoreThemeSettings() SaveManager:SetIgnoreIndexes({}) InterfaceManager:BuildInterfaceSection(Tabs.SettingsTab) SaveManager:BuildConfigSection(Tabs.SettingsTab) Window:SelectTab(1) Fluent:Notify({ Title = "Night Mystic", Content = "Carregado com sucesso! Farm Level + Kill Nearby + Bring + Tween+Stop + Chest/Fruit + ESP + NoFog", Duration = 8 }) --========================[ BOTÃO FLUTUANTE (ABRIR/FECHAR HUB) ]========================-- do -- cria um ScreenGui separado pra nunca ser escondido junto com o hub local ToggleGui = Instance.new("ScreenGui") ToggleGui.Name = "NM_Toggle" ToggleGui.ResetOnSpawn = false ToggleGui.IgnoreGuiInset = true ToggleGui.DisplayOrder = 999999 ToggleGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling ToggleGui.Parent = GuiRoot -- botão (arrastável) local Btn = Instance.new("TextButton") Btn.Name = "OpenClose" Btn.Size = UDim2.new(0, 46, 0, 46) Btn.Position = UDim2.new(0, 18, 0.5, -23) Btn.BackgroundColor3 = Color3.fromRGB(28, 28, 36) Btn.AutoButtonColor = true Btn.Text = "㊰" Btn.TextScaled = true Btn.Font = Enum.Font.GothamBold Btn.TextColor3 = Color3.fromRGB(255,255,255) Btn.Parent = ToggleGui local corner = Instance.new("UICorner", Btn) corner.CornerRadius = UDim.new(0, 12) local stroke = Instance.new("UIStroke", Btn) stroke.Thickness = 1 stroke.Color = Color3.fromRGB(90,90,110) -- dica local Tip = Instance.new("TextLabel") Tip.BackgroundTransparency = 1 Tip.Size = UDim2.new(0, 120, 0, 18) Tip.Position = UDim2.new(0, 0, 1, 2) Tip.Text = "" Tip.TextScaled = true Tip.Font = Enum.Font.Gotham Tip.TextColor3 = Color3.fromRGB(210,210,220) Tip.Parent = Btn -- lógica de arrastar local dragging, dragStart, startPos local function updateDrag(input) local delta = input.Position - dragStart Btn.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) end Btn.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = true dragStart = input.Position startPos = Btn.Position input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end) end end) Btn.InputChanged:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then if dragging then updateDrag(input) end end end) UserInputService.InputChanged:Connect(function(input) if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then updateDrag(input) end end) -- função de toggle robusta (tenta várias formas) local function toggleHub() -- 1) se pegamos o ScreenGui do Fluent, alterna Enabled if FluentGui and FluentGui.Parent then FluentGui.Enabled = not FluentGui.Enabled return end -- 2) tenta minimizar/restaurar pela API do Fluent (se existir) if Window and typeof(Window.Minimize) == "function" then Window:Minimize() return end if Window and typeof(Window.Toggle) == "function" then Window:Toggle() return end -- 3) fallback: tenta qualquer ScreenGui com "Fluent" no nome for _,g in ipairs(GuiRoot:GetChildren()) do if g:IsA("ScreenGui") and g ~= ToggleGui and (string.find(g.Name, "Fluent") or string.find(g.Name, "Night") or string.find(g.Name, "Mystic")) then g.Enabled = not g.Enabled return end end end Btn.MouseButton1Click:Connect(toggleHub) end